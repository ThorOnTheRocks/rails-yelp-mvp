<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <h1 style="text-align: center">Restaurants</h1>
		<%= link_to "Create new restaurant", new_restaurant_path, class: "btn btn-primary btn-sm"  %>
  </div>
</nav>


<div class="container">
	<div class="row mt-5">
		<% @restaurants.each do |restaurant| %>
			<div class="card m-4" style="width: 18rem;">
				<div class="card-body">
					<h5 class="card-title"><%= restaurant.name %></h5>
					<p class="card-text"></p>
					<%= link_to "Go restaurant info", restaurant_path(restaurant.id), class: "btn btn-primary" %>
				
					<%# <img src="..." class="card-img-top" alt="..."> %>
				</div>
			</div>
					
					
		<% end %>
		
	</div>
</div>



