<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
  <%= link_to "Go back", restaurant_path(@restaurant.id), class: "btn btn-primary" %>
<% end %>